!!! 5
%head
  %title
    - if content_for? :title
      = yield :title
      |
    Diffux
  = stylesheet_link_tag 'application', media: 'all'
  = javascript_include_tag 'application'
  = csrf_meta_tags
%body
  %nav.navbar.navbar-default{ role: 'navigation' }
    .container
      %ul.nav.navbar-nav
        %li= link_to 'Diffux', root_path, class: 'navbar-brand'
        %li= link_to 'Projects', projects_path
        %li= link_to 'About', static_pages_about_path
      %ul.nav.navbar-nav.navbar-right
        - if current_user.nil?
          %li= link_to 'Login', google_oauth2_path
        - else
          %li
            = image_tag current_user.image_url, class: 'navbar-user-image'
          %li
            = link_to '#', data: { toggle: 'dropdown' } do
              = current_user.name
              %b.caret
            %ul.dropdown-menu
              %li= link_to 'Logout', logout_sessions_path, data: { method: 'delete' }

  .container
    - if notice
      .alert.alert-success= notice
    = yield
